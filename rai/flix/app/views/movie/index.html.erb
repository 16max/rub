<h1>Movies</h1>
<table>
	<tr>
		<th>Titel</th>
		<th>Rating</th>
		<th>Umsatz</th>
		<th>Release</th>
		<th>Beschreibung</th>
	</tr>
	<% @movies.each do |movie| %>
	<tr>
		<td><%= movie.titel %></td>
		<td><%= movie.rating %></td>
		<td><%= event.total_gross.zero? ? "FREE" : number_to_currency(movie.total_gross, unit: "€ ") %></td>
		<td><%= movie.release %></td>
		<td><%= truncate(movie.description, length: 10, seperator: "") %></td>
	</tr>
	<% end %>
</table>